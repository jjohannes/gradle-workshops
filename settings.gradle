pluginManagement {
    repositories {
        gradlePluginPortal()
        // maven {
        //     url = "https://mycompany.com/repo"
        // }
    }
    includeBuild("gradle/plugins")
}
plugins {
    // ...
}

dependencyResolutionManagement {
    repositories {
        mavenCentral()
        // maven {
        //     url = "https://mycompany.com/repo"
        // }
    }
    // includeBuild("../other-gradle-project")

    // versionCatalogs {
    //     create("libs") {
    //         library("commons.lang3", "org.apache.commons:commons-lang3:3.6")
    //     }
    // }
}

rootProject.name = 'gradle-workshop'
include(':app', ':business-logic', ':data-model')

// project(':app').projectDir = file("components/app")

// buildCache {
//
// }
