plugins {
    id 'my-java-base'
    id 'java-library'
    id 'maven-publish'
}

publishing {
    publications.create("maven", MavenPublication).from(components["java"])

    repositories {
        maven {
            url = "../test-repo"
        }
    }
}
